using UnityEngine;

/*
[CreateAssetMenu(menuName = "State", fileName = "New State")]
public class State : ScriptableObject
{
    public BoolReference[] BlockingActions;

    public bool CanExecute
    {
        get
        {
            for (int i = 0; i < BlockingActions.Length; i++)
            {
                if (BlockingActions[i])
                    return false;
            }
            return true;
        }
    }
}
*/
